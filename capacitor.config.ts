import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fractionCalculationsApp',
  webDir: 'www'
};

export default config;
